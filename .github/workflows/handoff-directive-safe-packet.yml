name: Handoff directive safe packet

on:
  push:
    branches:
      - agent/handoff-continuity-checkpoint
    paths:
      - 'scripts/apply-handoff-directive-safe-packet.py'
      - '.github/workflows/handoff-directive-safe-packet.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/handoff-continuity-checkpoint
          fetch-depth: 0

      - name: Apply verification-safe handoff patch
        run: python scripts/apply-handoff-directive-safe-packet.py

      - name: Restore
        run: dotnet restore GPTDeskTop.sln

      - name: Validate runtime regression suite
        run: dotnet test tests/GPTDeskTop.RuntimeTests/GPTDeskTop.RuntimeTests.csproj --configuration Release --no-restore

      - name: Remove one-shot integration files
        if: success()
        shell: bash
        run: |
          rm -f scripts/apply-handoff-directive-safe-packet.py
          rm -f .github/workflows/handoff-directive-safe-packet.yml

      - name: Commit integrated fix
        if: success()
        shell: bash
        run: |
          if git diff --quiet; then
            echo 'No integration changes required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: preserve recovery directive in handoff checkpoint packet'
          git push origin HEAD:agent/handoff-continuity-checkpoint
