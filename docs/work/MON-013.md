# MON-013 — Live Chrome Target Revalidation Inside Start Boundary

Issue: #136

Implementation branch: `agent/mon-013-live-target-revalidation`

Status: **DONE / VERIFIED / MERGED / STABLE PUBLISHED**

## Objective

Prevent a queued Start from creating a worker against a Chrome target whose live conversation identity changed or disappeared after the caller's pre-gate tab snapshot was captured.

## Root cause

`StartMonitorAsync` validated `monitor.Url` against the caller-supplied `ChromeTab.Url` before waiting for the per-monitor lifecycle gate. The `ChromeTab` came from an earlier Chrome `/json/list` snapshot. A target can navigate or disappear while Start waits, while later CDP commands still use that target/WebSocket. The pre-gate URL check therefore could become stale before worker creation.

MainForm also persisted runtime target ID/title directly before entering the monitor-service Start boundary, leaving runtime target metadata mutation outside the lifecycle serialization used for Start/Stop/Delete/settings.

## Implementation contract

1. Start retains the fast pre-gate caller identity validation.
2. After acquiring the lifecycle lease and reloading the persisted monitor row, Start calls Chrome `/json/list` again.
3. The requested target is resolved by exact Target ID from the fresh list.
4. Start is rejected before `_running.Add` if the target disappeared.
5. Start is rejected if the fresh target is no longer a stable ChatGPT conversation URL.
6. Start is rejected if the fresh target conversation identity differs from the persisted monitor identity.
7. Runtime target ID/title persistence is performed inside the service Start lifecycle boundary using the expected persisted conversation URL guard.
8. The persisted monitor snapshot is updated only with the fresh target ID/title after that guarded database update succeeds.
9. Worker creation uses `MonitorLoopAsync(persistedMonitor, liveTab, ...)`, including the fresh WebSocket debugger URL and title.
10. MainForm only resolves the intended open tab and delegates Start; it no longer writes runtime target metadata directly.
11. MON-012 fresh persisted configuration semantics remain unchanged.

## Regression coverage

`MonitorLiveTargetRevalidationTests.cs` provides deterministic real-SQLite + fake Chrome-list coverage for:

- a target that navigates to another conversation before Start is rejected and cannot alter saved target metadata;
- a target that disappears before Start is rejected;
- a fresh same-conversation target commits its current target/title metadata;
- source ordering requires fresh Chrome-list read and live identity validation after the lifecycle lease/persisted row and before target commit/worker creation;
- the worker explicitly receives `liveTab`;
- MainForm no longer performs the runtime target database update.

`MonitorPersistedStartConfigurationTests.cs` uses a deterministic Chrome-list HTTP handler so MON-012 fresh-configuration tests exercise the new live-target boundary without depending on a local Chrome instance.

`OperatorStartConversationIdentityTests.cs` keeps its original identity-safety guarantees while asserting service-owned runtime target persistence.

## QA compatibility hardening discovered by CI

MON-013 correctly exposed stale assumptions in the legacy Passive Chat Wait process probe. Production live-target validation was not weakened.

The QA-only corrections were:

1. Map only the probe's two physical Target IDs to stable synthetic `/c/...` identities in `/json/list`, while preserving the real WebSocket targets.
2. Preserve Chrome's camelCase `/json/list` schema (`id`, `title`, `url`, `type`, `webSocketDebuggerUrl`) after rewriting the URLs.
3. Launch the isolated QA Chrome deterministically/headless using structured `ArgumentList` instead of shell command-line parsing.
4. Preserve assertion diagnostics by collecting failures, printing monitor running-state/activity traces, then failing the workflow.
5. Inject the explicit current error at 10 seconds while the slow response remains at 40 seconds, so the test still crosses the historical 30-second watchdog threshold without relying on a narrow stable-error timing edge.
6. Count explicit-error refresh activity using the committed live Chrome title. The diagnostic trace proved production emitted `Error saved. Refreshing only this tab...`; the earlier zero count was caused only by searching the obsolete pre-Start saved title.

The final passive probe verifies:

- the healthy/generating slow chat reaches `slow-complete-load-1` without an elapsed-time refresh;
- the explicit current error reaches `error-recovered-load-2` after exactly one error-driven refresh;
- `ErrorRefreshActivityCount = 1`;
- `ElapsedTimeRefreshActivityCount = 0`;
- both monitor workers remain running through verification.

## Delivery and validation receipt

- Issue #136: **Closed / Completed**.
- PR #139: **Merged** — `fix(monitor): revalidate live Chrome target before Start`.
- Final PR head: `512feb03e0bc1a5a6fd3ca759438c8071ce54115`.
- Main implementation merge: `cb2488a19c919f939baaba0ecb41535b03074f59`.

### PR validation — 8/8 Green on the exact final head

- Build GPTDeskTop #530 — SUCCESS
- QA Passive Chat Wait #294 — SUCCESS
- QA Hidden Chrome CDP #300 — SUCCESS
- QA Crash Process Recovery #308 — SUCCESS
- QA Release x64 #318 — SUCCESS
- Development Delivery Receipts #408 — SUCCESS
- Development Task Recovery #404 — SUCCESS
- Development Message Reload #235 — SUCCESS

Build #530 passed runtime automation, work-window lifecycle, runtime binding/integration, delivery invariants, multi-monitor delivery, saved-monitor rebinding, CDP reliability, crash recovery, application build, setup build, helper build, and rotation safety.

### Main validation — 8/8 Green on the exact implementation merge

GitHub Actions reported **8 successful push-triggered workflows and zero failures** for `cb2488a19c919f939baaba0ecb41535b03074f59`, covering the same required workflow set:

- Build GPTDeskTop #532 — SUCCESS
- QA Passive Chat Wait #296 — SUCCESS
- QA Hidden Chrome CDP — SUCCESS
- QA Crash Process Recovery #310 — SUCCESS
- QA Release x64 #320 — SUCCESS
- Development Delivery Receipts — SUCCESS
- Development Task Recovery — SUCCESS
- Development Message Reload #237 — SUCCESS

## Stable publication receipt

The automatic `Update Last release` workflow #31 completed successfully for the MON-013 implementation merge and produced release commit:

`c2fcb82d8907bedc13c7a3f0a150289a64a6e381` — `chore(release): update Last release [skip ci]`

`Last release/RELEASE.txt` records:

- File: `GPTDeskTop.exe`
- Version: `1.8.0.0`
- Stable build ID: `cb2488a1`
- Informational version: `1.8.0+stable.cb2488a1.cb2488a19c919f939baaba0ecb41535b03074f59`
- Platform: Windows x64
- Configuration: Release / self-contained / single-file / compressed
- Source commit: `cb2488a19c919f939baaba0ecb41535b03074f59`
- Generated UTC: `2026-08-10T11:57:12Z`
- Validation: `8/8 required GitHub Actions workflows passed for the same source commit`
- SHA-256: `c5ccb2d53ca2d2661cb19f270cc64a05ffcafe9a9138a8f86c85a412efc83bb5`

## Compatibility

No automatic navigation or rebinding was introduced. No production DOM selector, passive long-response waiting semantics, delivery verification, recovery algorithm, rotation algorithm, SQLite schema, configuration import/export, Setup behavior, or stable-release publisher behavior was intentionally changed by MON-013.
