# UI-007 — MainForm runtime-status font resource lifecycle

Issue: #112

Implementation branch: `agent/ui-007-mainform-status-font`

Status: **IN PROGRESS**

## Objective

Remove the remaining per-formatting GDI-backed `Font` allocation from the Saved Monitors Runtime column while preserving the existing Running/Stopped visual presentation.

## Root cause

UI-006 removed per-formatting font allocation from `HomeMetricsService`, but `MainForm.FormatMonitorCell` independently continued to create a new bold `Font` on every `DataGridView.CellFormatting` callback. Repaint, scrolling, selection/invalidation and live runtime-state refreshes can therefore create repeated GDI font allocations during long-running operation.

## Implementation contract

1. MainForm owns one bold Runtime-status font for its lifetime.
2. `FormatMonitorCell` reuses that owned font and contains no `new Font(...)` allocation.
3. Running/Stopped colors and typography remain unchanged.
4. The owned font is disposed during the MainForm shutdown lifecycle.
5. Regression coverage locks both no-per-paint allocation and explicit resource disposal.
6. No monitoring, CDP, recovery, delivery, SQLite, setup or release-publisher behavior changes.

## Validation required before merge

The exact final PR head must pass all eight established repository workflows before merge. After merge, the same main source commit must pass the 8/8 stable gate set before `Last release` can advance.
