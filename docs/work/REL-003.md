# REL-003 — Root Last release Stable EXE

Issue: #95

Implementation branches: `agent/rel-001-last-release`, `agent/rel-001-main-message-reload-gate`, `agent/rel-001-compress-stable-exe`

Status: **DONE / VERIFIED / PUBLISHED**

## Objective

Keep a root-level `Last release` folder that always points operators to the newest verified stable Windows x64 GPTDeskTop application executable.

## Delivered contract

1. The primary file is `Last release/GPTDeskTop.exe`.
2. The executable is published as Release / win-x64 / self-contained / single-file / compressed.
3. Publication is allowed only for a `main` source commit.
4. The same source SHA must pass all eight required stable workflows before publication.
5. `RELEASE.txt` records source SHA, file version, generation time, 8/8 validation and SHA-256.
6. Publication aborts if `main` advances while the executable is being prepared.
7. The generated release-only commit uses `[skip ci]` so it cannot recursively trigger another publication cycle.
8. Stale publisher runs are cancelled when a newer `main` candidate appears.
9. The publisher refuses an executable at or above the repository safety threshold instead of creating an invalid Git commit.

## Implementation receipts

- PR #96 — initial verified `Last release` publisher and regression contract.
- PR #97 — made Development Message Reload a same-SHA `main` gate and cancelled stale publisher candidates.
- PR #98 — enabled .NET single-file compression after the first self-contained EXE measured 164,207,744 bytes.
- Verified stable source commit: `7467a148f0e107a017941f87cc029464db1a3ccb`.
- Automated release commit: `d946fe7fa52d027291b28fb7f90548b9d1add6fb`.
- Initial stable executable size: 72,790,888 bytes.
- Initial stable executable SHA-256: `293565c6e3b66152620c233649174b7e83d81d92ebc86f0e611dc5ad27e9cf1e`.
- Issue #95 closed Completed after the executable and receipt were confirmed on `main`.

## Required stable workflows

- Build GPTDeskTop
- QA Release x64
- QA Hidden Chrome CDP
- QA Crash Process Recovery
- QA Passive Chat Wait
- Development Delivery Receipts
- Development Task Recovery
- Development Message Reload

## Current publication receipt

- Source commit: `4e27756e8ea6e5c003f73e320e253c7d44371d09`
- Published by commit: `2f840fb`
- Version: `1.8.0.0`
- Stable build ID: `4e27756e`
- Validation: all 8 required workflows passed for the same source commit
- SHA-256: `af0561bec21aa85838cade83b268b8fa1f4cfd9c897e1e0c18ac8031e79f6bf2`

## Regression coverage

`LastReleasePublisherRegressionTests.cs` locks the gate list, same-SHA requirement, x64 compressed single-file publication, `Last release` path, SHA-256 receipt, stale-main guard and recursion guard.
