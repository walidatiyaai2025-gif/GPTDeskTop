# UI-009 — MainForm monitor subscription and owned resource lifecycle

Issue: #119 — **COMPLETED**

Implementation branch: `agent/ui-009-mainform-resource-lifecycle`

Implementation PR: #130

Status: **DONE / VERIFIED / MERGED / STABLE PUBLISHED**

## Objective

Make MainForm explicitly release long-lived monitor-service event subscriptions and manually-owned UI resources during teardown, including direct-dispose and future form-recreation paths.

## Root cause

MainForm subscribed to `ChatGptMonitorService.Activity`, `HistoryChanged` and `RunningStateChanged` using anonymous lambdas. Those delegates could not be removed later and the monitor service could therefore retain callbacks targeting a torn-down MainForm. MainForm also owns a manually-created `ToolTip`, while UI-007's status font cleanup sat in `OnFormClosing` instead of the form's resource ownership boundary.

## Delivered implementation

1. Replaced the three anonymous monitor-service subscriptions with named handlers.
2. Each named subscription has a matching unsubscribe in `Dispose(bool)`.
3. Named handlers retain the existing `Ui(...)` shutdown/disposal guard before touching controls.
4. MainForm-owned `ToolTip` and Runtime-status `Font` are disposed exactly once through `Dispose(bool)`.
5. `OnFormClosing` remains responsible only for graceful async shutdown sequencing, not resource disposal.
6. Existing activity, stored-history and running-state refresh behavior is preserved.
7. Regression coverage locks named subscribe/unsubscribe symmetry and the explicit once-only resource lifecycle.
8. No monitoring semantics, CDP behavior, recovery, delivery, SQLite schema, setup or release-publisher behavior changed.

## Verified receipts

- Issue: #119 — closed as Completed.
- Implementation PR: #130 — merged by squash.
- Verified implementation head: `f8422947067d8df37630654b798770329d3a70e5`.
- Main merge commit: `fb3930b05f2d9dcdbdd24a7b19990c85a2f177d2`.
- Duplicate implementation PR #131 was intentionally closed without merge after the earlier canonical #130 was found Green, avoiding competing MainForm changes.

### PR-head validation

All eight PR-triggered workflows passed on the exact implementation head:

- Build GPTDeskTop #502
- QA Release x64 #290
- QA Passive Chat Wait #266
- QA Hidden Chrome CDP #272
- QA Crash Process Recovery #280
- Development Delivery Receipts #380
- Development Task Recovery #376
- Development Message Reload #207

### Main validation

The merged main commit `fb3930b05f2d9dcdbdd24a7b19990c85a2f177d2` also passed all eight push-triggered workflows:

- Build GPTDeskTop #504 — runtime automation, development lifecycle/delivery/rebinding/CDP/crash invariants, application build, Setup build, helper build and rotation-safety validation all passed.
- QA Release x64 #292
- QA Passive Chat Wait #268
- QA Hidden Chrome CDP #274
- QA Crash Process Recovery #282
- Development Delivery Receipts #382
- Development Task Recovery #378
- Development Message Reload #209

### Stable publication

The stable publisher advanced `Last release` from the verified UI-009 main source:

- Stable build ID: `fb3930b0`
- Source commit: `fb3930b05f2d9dcdbdd24a7b19990c85a2f177d2`
- Informational version: `1.8.0+stable.fb3930b0.fb3930b05f2d9dcdbdd24a7b19990c85a2f177d2`
- Validation: 8/8 required workflows passed for the same source commit
- SHA-256: `e1ef49be04a6cc572917afa39b77f2607b00d7c48c1825d87b302d9ef2dae845`
- Publisher commit on main: `837cedbf3f3fb1094942ab36a8c84c637fa6c767`

## Compatibility

No monitoring semantics, Start/Stop/Delete lifecycle behavior, ChatGPT DOM/CDP behavior, recovery, conversation rotation, delivery verification, SQLite schema, configuration import/export, Setup or stable-release publisher contract was changed by UI-009.
